<IfModule mod_rewrite.c>
    RewriteEngine On

    # (Removed parameter stripping to prevent redirect loops)

    # Forward everything to public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
